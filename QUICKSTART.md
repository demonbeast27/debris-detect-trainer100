# 🚀 Quick Start Guide

## Installation

```bash
# Navigate to project root
cd C:\Users\tilak\Downloads\debrisPredictfinal

# Install dependencies
pip install -r requirements.txt
```

## Running the Application

```bash
# Navigate to app directory
cd debrisPredict\debrisPredict

# Run the application
python main.py
```

## Access the Application

Open your browser and go to:
```
http://localhost:5000
```

## Stop the Application

Press `CTRL+C` in the terminal

---

## Alternative Commands

**If you need to use a different port:**
```bash
python main.py --port=5001
```

**If pip doesn't work, try pip3:**
```bash
pip3 install -r requirements.txt
python3 main.py
```

**To find your IP for network access:**
```bash
ipconfig
```

Then access from other devices at: `http://<your-ip>:5000`

---

That's it! The application should now be running. For detailed features and troubleshooting, see `UPDATES_README.md`.